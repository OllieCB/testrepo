<!doctype html>
<html lang="en">
	<head>
		{{> includes/headBlock}}

		<!-- Set title -->
		<title>{{#if title}}{{title}}{{/if}}</title>
	</head>
	<body>
		{{#> content}}
			Optional default content
		{{/content}}
	</body>
</html>
